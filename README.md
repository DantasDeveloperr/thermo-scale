# Thermo-Scale

Este é um projeto em Go que representa meus primeiros passos na linguagem, criado como parte dos meus estudos e práticas de programação.

---

## 📌 Sobre o Projeto

O **Thermo-Scale** é um projeto simples para exercitar conceitos básicos em Go tais como:
- definição de funções
- manipulação de entrada/saída
- lógica em console

O objetivo principal é servir como um repositório de estudos e prática da linguagem Go, mostrando minha evolução com o tempo.

---

## 🚀 Pré-requisitos

Antes de rodar o projeto localmente, você precisa ter instalado:

- Go (versão recomendada: 1.20+)
- Git (para controle de versão)

---

## 🧠 Estrutura do Repositório

O repositório está organizado com arquivos de código `.go` e um `.gitignore` para manter a limpeza do projeto.

Alguns pontos importantes:
- O arquivo principal de exemplo é `thermo.go`
- O arquivo `thermo.exe` não está versionado (foi removido do controle de versão usando `.gitignore`)

---

## 📌 Passo a passo (Git Bash)

Aqui estão os comandos que usei para inicializar e subir o projeto no GitHub:

1. **Inicializar o repositório Git**
    ```bash
    git init
    ```

2. **Adicionar arquivos e preparar o commit**
    ```bash
    git add .
    ```

3. **Criar um commit**
    ```bash
    git commit -m "Primeiro commit com README e código Go"
    ```

4. **Adicionar o repositório remoto**
    ```bash
    git remote add origin https://github.com/DantasDeveloperr/thermo-scale.git
    ```

5. **Enviar para o GitHub**
    ```bash
    git push -u origin main
    ```

Agora a branch `main` está rastreando `origin/main` e você pode apenas usar:
```bash
git push
